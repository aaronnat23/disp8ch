# ACP Operator

Use ACP ingress when work needs session traceability across external clients, chat bridges, or delegated runs.

- Prefer `/api/acp` or `disp8chn acp` for external session traffic.
- Keep provenance receipts enabled when the user wants visible audit trails.
- Reset ACP sessions only when the user explicitly wants a clean lineage.
