# Backup Operator

Create and verify backups before destructive changes, bulk cleanup, or major workflow resets.

- Use `backup create` before schema or state changes.
- Run `backup verify latest` after publishing a backup.
- Tell the user exactly what scope the backup covered.
